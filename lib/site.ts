/**
 * Canonical origin for absolute URLs (OG tags, canonicals, share links).
 * Set NEXT_PUBLIC_SITE_URL in Vercel when the site moves to its real domain.
 */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://aienergycouncil.vercel.app";
